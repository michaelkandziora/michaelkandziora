# Herkunft

`assets/portrait.txt` enthält den PRE-Text deiner ursprünglichen Webarchive unverändert, einschließlich äußerer Leerzeichen. Beim Rendern werden ausschließlich gemeinsame äußere Leerränder entfernt; die relativen Abstände und alle Bildzeichen bleiben erhalten. Auch die unteren Bildzeilen der Webarchive sind enthalten, die in der späteren README-Kopie fehlten.

Die statischen Profilangaben stammen aus `readme_template(1).md`. Uptime bleibt der dort eingetragene statische Wert; es wurde kein Geburtsdatum abgeleitet. Stats wurden nicht erfunden: Vor dem ersten erfolgreichen Abruf steht `n/a`.

SHA-256 des unveränderten PRE-Texts:

```text
446848e0a554464e6f420a013299997d0c56fa74023872ab51da5731b16734c3
```
